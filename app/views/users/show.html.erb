<h1><%= @user.first_name + ' ' + @user.last_name %></h1>
<h4><%= @user.email %></h4>
<%= link_to 'Back', users_path, class: "btn"  %> <%= link_to 'Edit', edit_user_path(@user), class: 'btn' %> <%= link_to 'Delete', user_path(@user),
        method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn' %>